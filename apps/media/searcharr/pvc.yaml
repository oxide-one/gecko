apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: searcharr-config
spec:
  storageClassName: "ceph-nvme"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
status: {}